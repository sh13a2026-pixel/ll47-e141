# Runtime dependencies — chỉ những gì app CẦN khi CHẠY trên thiết bị.
# flet-cli là công cụ BUILD, KHÔNG đưa vào đây (sẽ bị bundle vào APK lãng phí).

# Flet framework runtime (Flutter ↔ Python bridge)
flet==0.25.2

# Socket.io realtime — firestore_client.py dùng; nếu thiếu tự fallback polling REST
python-socketio[client]>=5.11.0
websocket-client>=1.7.0

# Pillow bị loại khỏi Android để giảm ~40MB và tăng tốc khởi động.
# image_utils.py có fallback thuần Python nên app vẫn hoạt động đầy đủ.
# Uncomment nếu build desktop:
# Pillow>=10.2.0
